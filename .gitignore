__pycache__/
*.py[cod]
*.so
build/
dist/
*.egg-info/
.vscode/
.pytest_cache/
.ruff_cache/

# Generated by the zeroing task, specific to one physical machine.
zero_offsets.json
zero_offsets.json.bad

# Bench results a task opts into via Config.save_json (tasks.result_path),
# e.g. speed_test -- also per-machine.
speed.json
speed.json.bad
cartesian_hand/tasks/_preview.py

# Tool-local metadata, not project content.
.rtk/

# Internal dev notes and agent scaffolding, not part of the public release.
# CLAUDE.md points at MEMORY.md and plan/, neither of which a clone has.
MEMORY.md
plan/
CLAUDE.md
.claude/

# Per-developer CMake config (local paths to vcpkg/python), not shared.
CMakeUserPresets.json

# Source clips for the README animations. The mp4s are tens of MB each and git
# keeps them forever; only the encoded media/*.webp belongs in the repo.
media/*.mp4
